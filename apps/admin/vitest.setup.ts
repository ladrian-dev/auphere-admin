import "@testing-library/jest-dom/vitest";

// Required by Better Auth's React client + components that use
// ``window.matchMedia`` (next-themes / sonner).
if (typeof window !== "undefined" && !window.matchMedia) {
  window.matchMedia = (query: string) => ({
    matches: false,
    media: query,
    onchange: null,
    addListener: () => {},
    removeListener: () => {},
    addEventListener: () => {},
    removeEventListener: () => {},
    dispatchEvent: () => false,
  });
}
