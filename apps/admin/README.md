# nexus-admin

Next.js 15 operator panel. Stub — full scaffold (Better Auth, Drizzle, shadcn, the Phase 1 pages) lands in block G.

Phase 1 pages (per BUILD-PLAN-v2):
- `/login`
- `/tenants`
- `/tenants/[id]` (overview + tool whitelist + channel status)
- `/tenants/[id]/agent` (agent_config editor: prompt, tools, policies, channels, versioning, promote)
- `/tenants/[id]/conversations` (live SSE + paginated history)
- `/tenants/[id]/isolation` (7 guarantees health dashboard)
- `/tool-catalog` (read)
