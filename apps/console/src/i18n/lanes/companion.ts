/**
 * Lane `companion`: los textos viven en `@nexus/companion-ui` desde la spec 003
 * (la consola y la aplicación de escritorio comparten el hilo). Aquí solo se
 * re-exportan para que `i18n/messages.ts` los mezcle como siempre.
 */
export { companionMessages } from "@nexus/companion-ui";
