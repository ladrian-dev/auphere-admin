# Workspace propio (patrón apps/admin): esta app no entra en el lockfile raíz.
packages:
  - "."
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
onlyBuiltDependencies:
  - esbuild
  - sharp
  - unrs-resolver
