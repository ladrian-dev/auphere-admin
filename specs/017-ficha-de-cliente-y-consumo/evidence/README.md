# Evidencia — spec 017

Una entrada por iteración (`iteracion-N.md`), con:

1. **Prototipo**: story en `packages/ui/src/stories/prototypes/`, captura y la aprobación del owner con fecha (R12.4).
2. **Suites**: salida resumida de `@nexus/ui`, consola, API (`-k console` + `tests/isolation`) y Playwright axe + overflow (R12.3).
3. **Paridad**: enlace a la sección de `parity.md` con el 100 % de las filas resueltas (R12.1).
4. **Recorrido**: crear cliente → cupo → agente → integraciones → operativo, en local y en staging tras el despliegue (R12.2).
5. **Lockfiles**: `pnpm-lock.yaml` y `uv.lock` sin cambios respecto a `develop` (T-LIC).
