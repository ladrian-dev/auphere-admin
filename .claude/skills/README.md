# Skills

Domain skills. Stubs land here as Phase 1 needs emerge.

Likely first skills:
- `seed-tenant` — runs `seed_<client>.py` and reports KG/agent_config delta.
- `run-isolation-suite` — runs `tests/isolation/` and summarizes failures.
- `promote-agent-config` — promotes a staged version with validations.
