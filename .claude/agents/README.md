# Agents

Specialized subagents. Stubs land here as Phase 1 needs emerge.

Likely first agents (block B+):
- `db-migrator` — drives Alembic migrations with safety checks.
- `isolation-reviewer` — reviews diffs touching tenant-scoping concerns.
- `mcp-author` — scaffolds new MCP servers from the tool catalog.
